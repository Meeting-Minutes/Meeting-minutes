DATABASE_URL=postgres://postgres:postgres@localhost:5432/minutes_dev
SESSION_SECRET=change-me-to-a-random-64-char-string
APP_URL=http://localhost:3000

# Outbound email (optional — required only for inviting accounts/shares by email)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@localhost
